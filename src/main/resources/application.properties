spring.application.name=multilanguage-platform

# Database Connection (Render-friendly: override via env vars)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d5ns5afgi27c738itcsg-a:5432/blogplatform_tuvn?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:blogplatform_tuvn_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Mb5sw979LEl9s4nXQeQrhmWdTyahIPZN}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=none

# server Port
server.port=8283

# Translation API Configuration
# Using LibreTranslate public endpoint (free, no API key required)
translation.api.url=https://libretranslate.de/translate
translation.api.key=

# Alternative: Using Argos (also free)
# translation.api.url=https://translate.argosopentech.com/translate

# Logging
logging.level.com.blog.multilanguage_platform.services.TranslationService=DEBUG