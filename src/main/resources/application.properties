spring.application.name=multilanguage-platform

# Database Connection - PostgreSQL (Render-ready)
# For local dev: Use local PostgreSQL or override with environment variables
# For Render: Set DATABASE_URL environment variable (automatically provided by Render)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d5ns5afgi27c738itcsg-a:5432/blogplatform_tuvn}
spring.datasource.username=${PGUSER:blogplatform_tuvn_user}
spring.datasource.password=${PGPASSWORD:Mb5sw979LEl9s4nXQeQrhmWdTyahIPZN}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Connection pool settings for remote database
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Server Port (Render will override with $PORT)
server.port=${PORT:8283}

# server Port
server.port=8283

# Translation API Configuration
# Using LibreTranslate public endpoint (free, no API key required)
translation.api.url=https://libretranslate.de/translate
translation.api.key=

# Alternative: Using Argos (also free)
# translation.api.url=https://translate.argosopentech.com/translate

# Logging
logging.level.com.blog.multilanguage_platform.services.TranslationService=DEBUG